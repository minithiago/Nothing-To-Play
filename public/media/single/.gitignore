# This folder is generated by pipeline/3-build-assets.mjs (npm run all)
# and feeds an optional experimental zoom layer (~10,000 small jpgs, ~160MB).
# Not committed to keep the repo lightweight; regenerate locally if needed.
*
!.gitignore
