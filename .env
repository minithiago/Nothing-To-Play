# Generated by pipeline/3-build-assets.mjs (npm run all inside pipeline/).
# Committed (no secrets here) because the build needs to know exactly how
# many texture-atlas layers and games shipped with this checkout of
# public/media and public/json. Re-running the pipeline regenerates this.
VITE_MEDIA_VERSION_0_LAYERS=1
VITE_MEDIA_VERSION_1_LAYERS=2
VITE_MEDIA_VERSION_2_LAYERS=47
VITE_GAME_COUNT=10000
